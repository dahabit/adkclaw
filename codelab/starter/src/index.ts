/**
 * AdkClaw — starter scaffold (Snapshot 0)
 *
 * You're at the empty canvas. The next four codelabs build the full agent
 * piece by piece:
 *
 *   Level 1 — Build the Brain      → adds src/agent/, src/tools/, src/sessions/, src/channels/, src/server/
 *   Level 2 — Memory & Skills      → adds src/memory/, src/context/compaction.ts, src/skills/
 *   Level 3 — The Agent Army       → adds src/multi-agent/, src/healing/, src/cron/
 *   Level 4 — Ship to the Cloud    → adds Dockerfile, src/storage/gcs.ts, Firestore session adapter
 *
 * Right now the only "live" pieces are:
 *   - src/types/   (all shared interfaces — pre-filled so you don't redefine them)
 *   - src/config/  (env + agent.yaml loader — pre-filled, tests pass)
 *   - src/cli/     (interactive `setup` wizard, REPL stub, CLI entrypoint — pre-filled)
 *
 * Try the wizard now:
 *
 *   npm install
 *   npm run setup        # interactive: name your agent, paste keys
 *   npm run typecheck    # tsc --noEmit — should be clean
 *
 * When you're ready, jump to Level 1 and start building the agent loop.
 */
console.log('🤖 AdkClaw scaffold v0 — start Level 1 to build the brain');
