# Daily note — 2026-05-08

Sample daily note. The agent appends a line per turn here using the
`daily_append` tool. The consolidator runs at ~3 AM local time and
promotes durable items into `bank/`.

This file is intentionally short — it'll grow as the day goes on.
