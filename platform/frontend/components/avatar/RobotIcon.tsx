/**
 * Re-export shim — old code imported from this path. The avatar component
 * is now CharacterIcon (painterly Imagen-generated portraits: boys, girls
 * with hijab, girls without hijab — see lib/avatars.ts).
 */

export { CharacterIcon as RobotIcon } from './CharacterIcon';
